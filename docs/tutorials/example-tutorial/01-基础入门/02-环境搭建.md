---
title: 1.2 环境搭建
date: 2025-01-01
categories:
  - 示例教程
tags:
  - 环境配置
  - 开发工具
---

# 1.2 环境搭建

## 系统要求

在开始之前，请确保你的系统满足以下要求：

- **操作系统**：Windows 10+ / macOS 10.15+ / Linux
- **内存**：至少 4GB RAM
- **磁盘空间**：至少 2GB 可用空间

## 安装步骤

### 步骤 1：安装基础工具

首先，我们需要安装基础工具：

```bash
# 检查是否已安装
tool --version

# 如果未安装，执行安装命令
# Windows
choco install tool

# macOS
brew install tool

# Linux
sudo apt-get install tool
```

### 步骤 2：配置环境变量

配置环境变量，确保工具可以在任何位置使用：

**Windows:**
```powershell
# 添加到系统环境变量 PATH
[Environment]::SetEnvironmentVariable("Path", $env:Path + ";C:\path\to\tool", "User")
```

**macOS/Linux:**
```bash
# 添加到 ~/.bashrc 或 ~/.zshrc
export PATH="$PATH:/path/to/tool"
```

### 步骤 3：验证安装

验证安装是否成功：

```bash
tool --version
# 应该输出版本号，例如：tool version 1.0.0
```

## 常见问题

### Q: 安装失败怎么办？

**解决方案**：
1. 检查系统要求是否满足
2. 确保有管理员权限
3. 查看错误日志，根据提示解决问题

### Q: 环境变量配置后不生效？

**解决方案**：
1. 重启终端或命令行窗口
2. 检查路径是否正确
3. 确保没有拼写错误

## 下一步

环境搭建完成后，让我们开始 [第一个示例](03-第一个示例.md) 吧！

---

**提示**：这是示例文章，展示了如何编写环境搭建教程。

