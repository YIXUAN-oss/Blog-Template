# 博客使用指南

> 本指南将帮助你快速上手并自定义这个博客模板

## 1. 快速开始

### 1.1 克隆项目

```bash
# 使用 GitHub CLI
gh repo clone YIXUAN-oss/Blog-Template my-blog

# 或使用 Git
git clone https://github.com/YIXUAN-oss/Blog-Template.git my-blog

cd my-blog
```

### 1.2 安装依赖

```bash
npm install
```

> 💡 **提示**：如果遇到依赖安装问题，可以尝试：
> ```bash
> npm install --legacy-peer-deps
> ```

### 1.3 本地开发

```bash
npm run dev
```

访问 `http://localhost:8080` 查看效果

> 💡 **提示**：如果端口被占用，可以指定其他端口：
> ```bash
> vuepress dev docs --port 8081
> ```

### 1.4 构建部署

```bash
npm run build
```

生成的静态文件在 `docs/.vuepress/dist` 目录

## 2. 首次配置

在开始使用前，请先完成以下配置：

### 2.1 修改个人信息

#### 2.1.1 修改配置文件

编辑 `docs/.vuepress/config.ts`，找到以下部分并修改：

```typescript
theme: recoTheme({
  author: '你的名字',                    // 修改为你的名字
  authorAvatar: '/avatar.png',          // 头像路径
  
  blog: {
    socialLinks: [
      { icon: 'BrandGithub', link: '你的GitHub链接' },
      { icon: 'Mail', link: 'mailto:你的邮箱' },
    ]
  },
})
```

#### 2.1.2 替换头像和 Logo

1. 将你的头像图片放到 `docs/.vuepress/public/` 目录，命名为 `avatar.png`
2. 将你的 Logo 图片放到 `docs/.vuepress/public/` 目录，命名为 `logo.png`

#### 2.1.3 修改首页信息

编辑 `docs/README.md`，修改以下内容：

- 标题和描述
- 个人信息卡片
- 联系方式

### 2.2 修改项目信息

编辑 `package.json`：

```json
{
  "name": "your-blog-name",
  "description": "你的博客描述",
  "author": {
    "name": "你的名字",
    "email": "你的邮箱"
  },
  "repository": {
    "url": "https://github.com/your-username/your-repo.git"
  }
}
```

### 2.3 配置部署路径

如果使用 GitHub Pages，需要修改 `docs/.vuepress/config.ts`：

```typescript
export default defineUserConfig({
  base: '/your-repo-name/',  // 修改为你的仓库名
  // ...
})
```

## 3. 添加内容

### 3.1 添加博客文章

在 `docs/blogs/` 目录下创建 Markdown 文件：

```markdown
---
title: 文章标题
date: 2025-01-01
categories:
  - 分类名称
tags:
  - 标签1
  - 标签2
author: 你的名字
---

# 文章标题

文章内容...

#### 3.1.1 图片使用

##### 3.1.1.1 本地图片

将图片放在 `docs/blogs/attachments/` 目录，然后引用：

```markdown
<img src="/blogs/attachments/image.jpg" alt="图片描述" style="border-radius: 8px;" />
```

#### 3.1 图床图片

推荐使用图床服务（GitHub、GitCode、jsDelivr 等）：

```markdown
<img src="https://cdn.jsdelivr.net/gh/username/repo@master/path/to/image.jpg" alt="图片描述" style="border-radius: 8px;" />
```

#### 3.2 日常随笔

在 `docs/diary/` 目录下创建文件，格式与博客文章相同。

### 3.3 文章 Front Matter 说明

每篇文章都需要在开头添加 Front Matter 配置：

| 字段 | 说明 | 必填 | 示例 |
|------|------|------|------|
| `title` | 文章标题 | ✅ | `title: 我的第一篇文章` |
| `date` | 发布日期 | ✅ | `date: 2025-01-01` |
| `categories` | 分类（数组） | ✅ | `categories: ['技术', '前端']` |
| `tags` | 标签（数组） | ✅ | `tags: ['Vue', 'JavaScript']` |
| `author` | 作者 | ❌ | `author: 你的名字` |
| `description` | 文章描述 | ❌ | `description: 这是一篇关于...` |
| `sticky` | 置顶（数字，越大越靠前） | ❌ | `sticky: 1` |

### 3.4 添加新的教程分类

1. 在 `docs/tutorials/` 下创建新目录，如 `docs/tutorials/go/`
2. 创建 `README.md` 作为教程首页（包含目录结构）
3. 创建子目录存放具体教程文章
4. 在 `docs/.vuepress/config.ts` 中添加导航配置（如需要）

示例教程目录结构：

```
docs/tutorials/
└── go/
    ├── README.md          # 教程首页
    ├── 01-基础/
    │   ├── README.md
    │   ├── 01-Go语言介绍.md
    │   └── 02-环境搭建.md
    └── 02-进阶/
        └── ...
```

## 4. 自定义配置

### 4.1 修改主题色

编辑 `docs/.vuepress/styles/palette.scss`：

```scss
// 主题色
$accentColor: #3eaf7c;  // 修改为你喜欢的颜色

// 其他颜色
$textColor: #2c3e50;
$borderColor: #eaecef;
$codeBgColor: #282c34;
```

### 4.2 修改导航栏

在 `docs/.vuepress/config.ts` 中修改 `navbar` 配置：

```typescript
navbar: [
  { text: '首页', link: '/', icon: 'Home' },
  { text: '博客', link: '/posts.html', icon: 'Blog' },
  { text: '教程', link: '/tutorials/', icon: 'Book' },
  { text: '关于', link: '/about/', icon: 'User' },
  // 添加更多导航项...
],
```

### 4.3 修改侧边栏

在 `config.ts` 中配置侧边栏：

```typescript
sidebar: {
  '/tutorials/': [
    {
      text: 'Java后端',
      children: [
        '/tutorials/java-backend/README.md',
        // 更多子页面...
      ]
    }
  ]
}
```

### 4.4 修改主题色（简化示例）

编辑 `docs/.vuepress/styles/palette.scss`：

```scss
$accentColor: #3eaf7c;  // 修改主题色
```

### 4.5 添加自定义样式

编辑 `docs/.vuepress/styles/index.scss`：

```scss
// 添加你的自定义样式
.my-custom-class {
  // ...
}
```

## 5. 多语言配置

### 5.1 添加英文内容

在 `docs/en/` 目录下创建对应的英文页面。

### 5.2 配置语言切换

在 `config.ts` 中配置：

```typescript
locales: {
  '/': {
    lang: 'zh-CN',
    title: '中文标题',
    description: '中文描述',
  },
  '/en/': {
    lang: 'en-US',
    title: 'English Title',
    description: 'English Description',
  }
}
```

## 6. 添加插件

### 6.1 评论系统

在 `docs/.vuepress/config.ts` 中配置评论（模板默认使用 Waline，你也可以切换到 Giscus 等）：

```typescript
theme: recoTheme({
  commentConfig: {
    // 方案一：使用 Giscus（GitHub Discussions）
    // type: 'giscus',
    // options: {
    //   repo: 'your-username/your-repo',
    //   repoId: '你的repo-id',
    //   category: 'Announcements',
    //   categoryId: '你的category-id',
    // },

    // 方案二：使用 Waline（与当前模板默认配置一致）
    type: 'waline',
    options: {
      serverURL: 'https://your-waline-domain.com', // 替换为你的 Waline 服务地址
      visitor: true,            // 是否开启浏览量统计
      dark: 'auto',             // 自动暗色模式
      locale: {
        placeholder: '请输入留言，填写邮箱可收到回复哦！...',
        website: '网址(http://)',
      },
    },
  },
})
```

### 6.2 统计分析

#### 6.2.1 选项1: Umami（推荐 - 自托管、隐私友好）

**第一步：部署Umami服务**

使用Docker部署：
```bash
# 使用PostgreSQL
docker run -d \
  --name umami \
  -p 3000:3000 \
  -e DATABASE_URL=postgresql://username:password@host:5432/umami \
  ghcr.io/umami-software/umami:postgresql-latest
```

或使用Vercel + Supabase免费部署：
1. Fork [Umami仓库](https://github.com/umami-software/umami)
2. 在Vercel导入项目
3. 在Supabase创建PostgreSQL数据库
4. 配置环境变量 `DATABASE_URL`

**第二步：添加网站并获取追踪代码**

1. 访问Umami控制台（如 `https://your-umami-domain.com`）
2. 登录后点击「Add website」
3. 填写网站信息，获取 **Website ID**
4. 复制追踪脚本URL（通常是 `https://your-umami-domain.com/script.js`）

**第三步：集成到博客**

在 `docs/.vuepress/config.ts` 中配置：

```typescript
head: [
  // 其他meta标签...
  
  // Umami 统计分析
  ['script', {
    async: true,
    src: 'https://your-umami-domain.com/script.js',  // 替换为你的Umami域名
    'data-website-id': 'your-website-id'             // 替换为你的Website ID
  }],
]
```

**优势：**
- ✅ 完全自托管，数据隐私可控
- ✅ 无需Cookie，符合GDPR
- ✅ 轻量级，不影响页面加载速度
- ✅ 界面简洁，数据清晰

#### 6.2.2 选项2: Google Analytics

在 `docs/.vuepress/config.ts` 中配置：

```typescript
head: [
  // Google Analytics 4
  ['script', { async: true, src: 'https://www.googletagmanager.com/gtag/js?id=G-XXXXXXXXXX' }],
  ['script', {}, `
    window.dataLayer = window.dataLayer || [];
    function gtag(){dataLayer.push(arguments);}
    gtag('js', new Date());
    gtag('config', 'G-XXXXXXXXXX');  // 替换为你的GA4 ID
  `],
]
```

#### 6.2.3 验证统计是否生效

1. 部署博客后访问页面
2. 打开浏览器开发者工具（F12）
3. 查看Network标签，搜索 `script.js` 或 `gtag`
4. 确认请求成功
5. 访问Umami/GA控制台，查看实时访问数据

## 7. 部署

### 7.1 GitHub Pages

1. 修改 `docs/.vuepress/config.ts` 中的 `base`：

```typescript
export default defineUserConfig({
  base: '/your-repo-name/',  // 仓库名（根据你实际仓库名修改，如 /my-blog/）
  // ...
})
```

2. 推送代码到 GitHub

3. 启用 GitHub Actions（已配置在 `.github/workflows/deploy.yml`）

4. 在仓库设置中启用 GitHub Pages，选择 `gh-pages` 分支

### 7.2 Vercel

1. 导入 GitHub 仓库到 Vercel

2. Vercel 会自动检测 `vercel.json` 配置

3. 点击部署即可

### 7.3 自定义域名

#### 7.3.1 GitHub Pages

1. 在 `docs/.vuepress/public/` 下创建 `CNAME` 文件

2. 写入你的域名，如：`blog.example.com`

3. 在域名提供商处添加 DNS 记录

#### 7.3.2 Vercel

在 Vercel 项目设置中添加自定义域名。

## 8. 资源管理

### 8.1 图片存储位置

- **博客文章图片**：`docs/blogs/attachments/`
- **教程图片**：`docs/tutorials/[分类]/attachments/`
- **静态资源**：`docs/.vuepress/public/`（Logo、头像等）

### 8.2 图片引用方式

#### 8.2.1 本地图片

```markdown
<!-- 博客文章中的图片 -->
<img src="/blogs/attachments/image.jpg" alt="描述" style="border-radius: 8px;" />

<!-- 静态资源图片 -->
<img src="/images/example.jpg" alt="描述" style="border-radius: 8px;" />
```

#### 8.2.2 图床图片（推荐）

使用 GitHub + jsDelivr：

```markdown
<img src="https://cdn.jsdelivr.net/gh/username/repo@master/path/to/image.jpg" alt="描述" style="border-radius: 8px;" />
```

### 8.3 图片处理工具

项目提供了图片处理脚本：

```bash
# 处理博客图片（添加圆角、分割线等）
python process_blog_images.py docs/blogs/

# 处理单个文件
python process_blog_images.py docs/blogs/文章名.md
```

### 8.4 替换静态资源

- **Logo**: `docs/.vuepress/public/logo.png`
- **头像**: `docs/.vuepress/public/avatar.png`
- **Favicon**: `docs/.vuepress/public/favicon.ico`

## 9. 目录结构说明

```text
Blog-Template/
├── docs/                      # 文档 & 内容目录
│   ├── .vuepress/             # VuePress 配置
│   │   ├── config.ts          # 配置文件（站点 & 主题主配置）
│   │   ├── client.ts          # 客户端增强逻辑（懒加载、留言板等）
│   │   ├── public/            # 静态资源（Logo、头像等）
│   │   └── styles/            # 样式文件（主题定制）
│   ├── blogs/                 # 博客文章
│   ├── tutorials/             # 教程中心
│   ├── diary/                 # 日常随笔
│   ├── friendship/            # 友情链接
│   ├── guestbook/             # 留言板
│   ├── en/                    # 英文首页及多语言内容
│   └── README.md              # 首页（中文）
├── .github/                   # GitHub 配置（Actions 流水线）
│   └── workflows/             # 自动部署等工作流
├── package.json               # 项目脚本 & 依赖配置
├── vercel.json                # Vercel 部署配置
└── README.md                  # 仓库说明（对外展示）
```

## 10. 关键配置文件说明

这一节概览项目中几个最重要的配置文件，方便你快速「对号入座」：

### 10.1 `package.json`（项目元信息与脚本）

- **主要配置点**：
  - `name`: 项目名称（npm 包名风格，建议小写中划线，如 `my-blog`）
  - `description`: 博客简介（用于 npm / 部署说明）
  - `author`: 作者名称和邮箱（展示在开源平台上）
  - `repository.url`: Git 仓库地址（用于 Issue / PR 跳转）
  - `scripts`:
    - `dev`: 开发环境启动脚本（默认 `vuepress dev docs`）
    - `build`: 构建静态文件（默认 `vuepress build docs`）
    - `serve`: 本地预览构建结果

> 修改建议：新建自己的仓库后，**第一时间**修改 `name`、`description`、`author`、`repository.url`。

### 10.2 `docs/.vuepress/config.ts`（站点 & 主题主配置）

- **站点信息**：

```ts
export default defineUserConfig({
  lang: 'zh-CN',
  title: '你的博客',
  description: '一个基于 VuePress 2 的个人博客模板',
  base: process.env.VERCEL || process.env.NODE_ENV === 'development'
    ? '/'
    : '/your-repo-name/',  // GitHub Pages 时改成你的仓库名
  // ...
})
```

- **主题信息（`recoTheme`）**：
  - `logo`: 站点 Logo 路径（`/logo.png`）
  - `author`: 显示在主题中的作者名
  - `authorAvatar`: 头像路径（`/avatar.png`）
  - `navbar`: 顶部导航菜单（文本、链接、图标）
  - `series`: 教程侧边栏/系列配置（本模板中多数设为空数组，侧边栏由主题自动生成或关闭）
  - `commentConfig`: 评论系统配置（默认 Waline，可改为 Giscus 等）

- **head 配置**（SEO & 统计等）：
  - `meta`：`keywords`、`author` 等基础 SEO 信息
  - `link`：`favicon`、CDN 预连接等
  - `script`：Umami / GA 等统计脚本（示例已给出，默认注释）

> 使用步骤：按需修改 `title` / `description` / `base`，再根据你的部署方案补充 head 中的统计脚本。

### 10.3 `docs/.vuepress/client.ts`（客户端增强逻辑）

这里是一些**与模板体验相关的增强功能**：

- 注册自定义组件：图标组件 `IconHome`、`IconBook` 等，以及友情链接组件 `FriendshipLinks`。
- 搜索框增强：
  - 为搜索框添加快捷键提示（`Ctrl K` / `⌘ K`）
  - 监听键盘事件，按快捷键自动聚焦搜索框
- 图片懒加载：
  - 为非关键图片添加 `loading="lazy"`
  - 使用 `IntersectionObserver` 渐进加载，并带有骨架样式（搭配 `index.scss`）
- 留言板页面增强：
  - 仅在 `/guestbook` 页面展示 Waline 评论区
  - 自动隐藏留言板页面右侧目录 / TOC
  - 调整 Waline 宽度，与正文信息块对齐
- 阅读进度条：
  - 页面顶部一条彩色进度线，反映阅读进度
- 首页个人信息卡片交互：
  - 右下角头像按钮点击弹出卡片

> 一般情况下**不需要修改这里的代码**，除非你要调整这些交互行为；如果要改 Waline 域名，可以搜索 `your-waline-domain.com` 统一替换。

### 10.4 首页配置：`docs/README.md` 与 `docs/en/README.md`

- Frontmatter 中的 `bannerBrand` 控制首页 Banner：
  - `title` / `description` / `tagline`：首页大标题、副标题、标语
  - `buttons`：主按钮配置（文本 + 链接）
  - `socialLinks`：GitHub / 邮箱等社交入口
- 页面主体中有：
  - 首页几何背景装饰
  - 右下角个人信息浮窗（头像 + 联系方式）
  - 教程卡片区（示例教程入口）
  - 关于 / CTA（引导 Star 与 About 页面）

> 使用方式：把 `你的名字`、`your-email@example.com`、`your-username` 等占位替换成自己的信息即可。

### 10.5 独立页面

- `docs/about/README.md`：**关于我** 模板，已为你预置完整结构（简介 / 技术栈 / 目标 / 项目 / 联系方式等），只需要按提示修改内容即可。
- `docs/friendship/README.md` & `docs/friendship/使用说明.md`：
  - 展示友情链接列表
  - 使用说明中有**如何添加友链卡片**与**申请格式**的详细说明。
- `docs/guestbook/README.md`：
  - 留言板入口页
  - 顶部文字引导 + GitHub Issues / Discussions 链接
  - 评论区由 Waline 提供，行为由 `client.ts` 控制。

### 10.6 内容目录

- `docs/blogs/`：博客文章（带 `attachments/` 存放图片）
- `docs/tutorials/`：教程中心，各类教程大纲与内容
- `docs/diary/`：日常随笔
- `docs/en/`：英文首页及未来扩展的英文内容

> 所有内容文件都是 Markdown，只要遵循 Front Matter 约定并放到对应目录，就能自动出现在博客 / 教程列表中。

## 11. 常见问题

### 11.1 本地开发时端口被占用

**解决方案**：修改启动端口

```bash
vuepress dev docs --port 8081
```

或在 `package.json` 中修改脚本：

```json
{
  "scripts": {
    "dev": "vuepress dev docs --port 8081"
  }
}
```

### 11.2 图片不显示

**可能原因**：
1. 图片路径错误
2. 图片未放在正确目录

**解决方案**：
- 使用绝对路径：`/blogs/attachments/image.jpg`
- 确保图片文件存在
- 检查图片文件名是否正确（区分大小写）

### 11.3 修改配置后不生效

**解决方案**：清除缓存并重启

```bash
# Windows
rmdir /s /q docs\.vuepress\.cache docs\.vuepress\.temp
npm run dev

# Linux/Mac
rm -rf docs/.vuepress/.cache docs/.vuepress/.temp
npm run dev
```

### 11.4 部署后样式错乱

**解决方案**：
1. 检查 `base` 配置是否正确（GitHub Pages 需要配置仓库名）
2. 确保构建成功：`npm run build`
3. 检查部署平台的输出目录配置

### 11.5 npm install 失败

**解决方案**：

```bash
# 清除缓存
npm cache clean --force

# 使用 legacy-peer-deps
npm install --legacy-peer-deps

# 或删除 node_modules 和 package-lock.json 后重新安装
rm -rf node_modules package-lock.json
npm install
```

### 11.6 搜索功能不工作

**解决方案**：
1. 确保已安装 `@vuepress/plugin-search`
2. 检查 `config.ts` 中是否正确配置了搜索插件
3. 重新构建项目

### 11.7 评论系统不显示

**解决方案**：
1. 检查评论配置是否正确（Giscus/Waline 等）
2. 确保已配置必要的参数（repo、repoId 等）
3. 检查网络连接（某些评论服务可能需要科学上网）

## 12. 学习资源

- [VuePress 官方文档](https://v2.vuepress.vuejs.org/zh/)
- [vuepress-reco 文档](https://theme-reco.vuejs.press/)
- [Markdown 语法](https://markdown.com.cn/)

## 13. 最佳实践

### 13.1 内容管理

1. **定期备份**: 使用 Git 管理内容，定期推送到远程仓库
2. **内容组织**: 保持目录结构清晰，使用合理的分类和标签
3. **持续更新**: 定期发布新内容，保持博客活跃

### 13.2 性能优化

1. **图片优化**: 
   - 压缩图片大小
   - 使用 WebP 格式
   - 使用图床服务（推荐）

2. **代码优化**:
   - 使用代码高亮
   - 避免过长的代码块

### 13.3 SEO 优化

1. **文章 Front Matter**: 为每篇文章添加合适的 title、description 和 tags
2. **URL 结构**: 使用有意义的文件名
3. **内部链接**: 在文章中添加相关文章的链接

### 13.4 写作建议

1. **结构清晰**: 使用清晰的标题层级
2. **代码示例**: 提供完整可运行的代码示例
3. **图片说明**: 为图片添加有意义的 alt 文本
4. **定期检查**: 定期检查链接是否失效

## 14. 项目结构说明

```
Blog-Template/
├── docs/                          # 文档目录
│   ├── .vuepress/                 # VuePress 配置
│   │   ├── config.ts              # 主配置文件
│   │   ├── client.ts              # 客户端配置
│   │   ├── public/                # 静态资源
│   │   │   ├── avatar.png         # 头像
│   │   │   ├── logo.png           # Logo
│   │   │   └── favicon.ico        # 网站图标
│   │   └── styles/                # 样式文件
│   │       ├── index.scss         # 全局样式
│   │       └── palette.scss       # 主题色配置
│   ├── blogs/                     # 博客文章
│   │   ├── attachments/            # 博客图片
│   │   └── *.md                   # 博客文章
│   ├── tutorials/                  # 教程中心
│   │   ├── java-backend/          # Java 后端教程
│   │   ├── python-ai/             # Python AI 教程
│   │   └── ...                    # 更多教程
│   ├── about/                     # 关于页面
│   ├── diary/                     # 日常随笔
│   ├── friendship/                # 友情链接
│   ├── en/                        # 英文内容
│   └── README.md                  # 首页
├── .github/                       # GitHub 配置
│   └── workflows/                 # GitHub Actions
├── process_blog_images.py         # 博客图片处理脚本
├── process_tutorial_images.py     # 教程图片处理脚本
├── package.json                   # 项目配置
├── vercel.json                    # Vercel 配置
└── README.md                      # 项目说明
```


## 15. 贡献

欢迎提交 Issue 和 Pull Request！

如果你发现任何问题或有改进建议，欢迎：
- 🐛 提交 [Issue](https://github.com/YIXUAN-oss/Blog-Template/issues)
- 💡 提交 [Pull Request](https://github.com/YIXUAN-oss/Blog-Template/pulls)
- ⭐ 给项目一个 Star

## 16. 许可证

[MIT License](LICENSE)

---

<div align="center">

**如果这个模板对你有帮助，请给一个 ⭐ Star 支持一下！**

如有问题，欢迎通过以下方式联系：

- 📧 邮件: [byyi.xuan@outlook.com](mailto:byyi.xuan@outlook.com)
- 🐙 GitHub: [@YIXUAN-oss](https://github.com/YIXUAN-oss)
- 💬 Issues: [提交问题](https://github.com/YIXUAN-oss/Blog-Template/issues)

</div>

